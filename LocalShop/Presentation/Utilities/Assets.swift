import Foundation
import SwiftUI

enum Assets {
    enum Color {
        static let primary = SwiftUI.Color("Primary")
        static let background = SwiftUI.Color("Background")
        static let button = SwiftUI.Color("Button")
    }
    
    enum Image {
        static let cart = SwiftUI.Image("cart-icon")
    }
}
